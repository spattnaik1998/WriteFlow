node_modules/
.env
*.log
.DS_Store
Thumbs.db

# Runtime state — never belongs in the repo
.essay-agent/
.ollama-runtime/

# Database snapshots — the full text of every note. Local only, never pushed.
backups/
